{$PANEL_DOMAIN} {
	reverse_proxy heliteb-panel:4321
}

{$API_DOMAIN} {
	# Solo necesario para que servicios externos (InboxCRM, Kommo, n8n) lleguen
	# a los webhooks - el panel habla con la API por la red interna de Docker,
	# no por este dominio.
	reverse_proxy heliteb-api:8080
}
